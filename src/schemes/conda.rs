use super::generic::{VersionParseScheme, VersionCompareScheme};

//pub struct CondaVersion<'a> {}

// impl VersionParseScheme<'a> for CondaVersion<'a> {

// }

// impl VersionCompareScheme<'a> for CondaVersion<'a> {

// }
